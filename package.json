{
  "name": "math-buddy",
  "version": "1.0.0",
  "description": "AI-powered math tutor for 5-year-olds, using ElevenLabs Conversational AI.",
  "main": "app.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html style.css app.js manifest.json sw.js www/ && cp -r icons www/ 2>/dev/null || true",
    "cap:init": "npx cap add ios",
    "cap:sync": "npm run build && npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "keywords": ["math", "tutor", "kids", "ai", "elevenlabs"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  }
}
